# tweets-rnn

This project tackles the “Real or Not? NLP with Disaster Tweets” challenge, where the task is to classify whether a tweet refers to a real disaster. The workflow combines classic NLP methods with deep learning approaches, ultimately building a Word2Vec-powered LSTM model that outperforms the baseline.
